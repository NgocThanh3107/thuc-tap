import React from "react";
import SharedForm from "./SharedForm";

const CreateForm = () => {
    return (
        <div>
            <SharedForm isEdit={false} />
        </div>
    );
};

export default CreateForm;
